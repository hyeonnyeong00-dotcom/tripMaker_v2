# Claude Code 마일스톤별 개발 프롬프트

## 사용 규칙 (토큰 효율의 핵심)

1. **마일스톤 1개 = 세션 1개.** 커밋 완료 후 `/clear`로 컨텍스트를 비우고 다음 마일스톤 시작.
2. 각 프롬프트는 그대로 복사해 입력. CLAUDE.md가 자동으로 읽히므로 배경 설명을 덧붙이지 말 것.
3. 프롬프트 첫 지시가 "계획 먼저"이므로, 계획이 마음에 들 때만 "진행해"라고 승인. (또는 Plan 모드 Shift+Tab 활용)
4. UI 마일스톤(M3, M5, M7, M8)은 정적 목업 확인 → 승인 → API 연결의 2턴 구조. 목업이 어긋나면 이 시점에 잡을 것 — 로직 연결 후 수정은 비용이 3배.
5. 사전 준비: 저장소 루트에 `CLAUDE.md`, `docs/PRD.md`, `docs/ERD.md`, `frontend/src/data/destinations.ts` 배치. Supabase 프로젝트 생성, Google Maps API 키·AI API 키 발급.

---

## M0. 프로젝트 셋업 + 스켈레톤 (Day 1 오전)

```
docs/PRD.md와 docs/ERD.md는 이미 저장소에 있어. CLAUDE.md 3절 구조대로 모노레포 스켈레톤을 만들자.

작업 범위:
1. frontend: Vite + React 18 + TypeScript 초기화, CLAUDE.md 2절의 라이브러리 설치, styles/tokens.css를 7절 토큰 그대로 생성, src/data/destinations.ts는 이미 있으니 건드리지 말 것
2. backend: Spring Boot 3 (Java 17, Gradle) 초기화 — web, data-jpa, security, validation, springdoc-openapi 의존성
3. backend/Dockerfile: 멀티스테이지 (gradle build → JRE 17 slim)
4. 루트에 .gitignore, .env.example (frontend/backend 각각, CLAUDE.md 8절 변수명)
5. docs/data-spec.md 생성: PRD 8.3 JSON 스키마를 복사하되 activity에 lat/lng 추가, 요청 바디에 include_nearby 추가 (CLAUDE.md 4절 참고)

구현 전에 생성할 파일 목록과 주요 설정을 계획으로 먼저 보여줘.
완료 기준: frontend dev 서버 기동, backend 부트 성공, docker build 성공, 커밋 1개.
```

## M1. Supabase 스키마 + 시드 (Day 1 오후)

```
docs/ERD.md의 7개 테이블을 Supabase에 생성하는 마이그레이션 SQL을 작성하자.

반드시 CLAUDE.md 5.6절 ERD 델타를 적용할 것:
- itinerary_activities에 lat, lng 컬럼 추가
- trips에 include_nearby 컬럼 추가
- ai_response_cache는 expires_at을 쓰지 않는 lazy expiry 전제

작업 범위:
1. backend/src/main/resources/db/migration/ 에 마이그레이션 SQL (ERD 5절 인덱스 포함)
2. 시드 SQL: admin 계정 1개(BCrypt 해시), prompt_templates 2행(initial_generation, reorder — 본문은 placeholder, M4/M6에서 채움)
3. backend에 JPA 엔티티 7개 + Repository 인터페이스 (로직 없이 매핑만)
4. Supabase 연결 설정 (application.yml, SUPABASE_DB_URL 환경변수)

계획 먼저. 완료 기준: 마이그레이션 실행 성공, backend가 Supabase에 연결된 상태로 부트, 커밋.
```

## M2. 인증/인가 (Day 2)

```
CLAUDE.md 4절의 auth 3개 엔드포인트와 JWT 인가를 구현하자.

작업 범위:
1. POST /api/auth/signup, /login, /logout — PRD 6.1 스펙 (BCrypt, JWT 만료 60분, 서버 토큰 저장 없음, logout은 200만 반환)
2. Spring Security 필터: JWT 검증, role 기반 인가 — /api/admin/** 은 admin만(403 FORBIDDEN), /api/trips** 는 인증 필수(401 AUTH_ERROR)
3. 에러 응답을 CLAUDE.md 4절 공통 형식으로 통일하는 @ControllerAdvice
4. frontend: axios 인스턴스(sessionStorage 토큰 자동 첨부, 401 시 로그인 페이지 리다이렉트), 로그인/회원가입 페이지 — CLAUDE.md 6절 로그인 행 스펙(브랜드 마크, 인사형 헤드라인, 검증 에러, 비밀번호 강도 바, 입력 완료 전 버튼 비활성)

계획 먼저. 완료 기준: 회원가입→로그인→JWT로 보호 API 호출, user의 admin API 접근 403, 커밋.
```

## M3. 일정 생성 폼 UI (Day 3) — 2턴 구조

**턴 1 (정적 목업):**
```
일정 생성 폼 페이지를 더미 데이터 정적 목업으로 먼저 만들자. API 연결은 다음 턴에 한다.

CLAUDE.md 6절 '일정 생성 폼' 행과 5.5절 목적지 선택 설계를 그대로 따를 것:
1. DestinationPicker 모달: 좌측 [국내|해외] 탭 → 우측 지역 리스트 → 상세(국내: 세부 지역 리스트 / 해외: 도시 칩) → 선택 시 닫힘. 상세 진입 시 뒤로가기 + breadcrumb. 데이터는 src/data/destinations.ts만 사용
2. 목적지 필드 아래 근교 포함 토글: NEARBY_MAP에 매핑 있으면 "교토·고베도 함께 볼까요?" 식 실제 지명, 없으면 폴백 문구. 스위치 토글
3. 기간 선택 시 "N박 M일" 자동 계산 배지
4. 예산: 자유 텍스트 입력 + 빠른 입력 칩(알뜰하게/적당히/플렉스 — 칩 클릭 시 텍스트 채움)
5. 취향 칩 다중 선택(먹방/힐링/액티비티/문화·역사/쇼핑/자연/카페/사진 명소) + "N개 선택됨" 표시
6. CTA 버튼에 목적지 반영("부산 일정 만들기"), 필수값 미입력 시 비활성
7. 생성 중 로딩: 단계 체크리스트(취향 분석→장소 후보→동선 계산→이동 시간) 목업

tokens.css 변수만 사용할 것. 계획 먼저, 구현 후 스크린샷으로 확인받고 커밋.
```

**턴 2 (연결은 M4 완료 후):** M4 프롬프트에 포함.

## M4. AI 일정 생성 백엔드 + 캐시 (Day 4)

```
POST /api/trips (최초 생성)를 구현하자. CLAUDE.md 5.3, 5.4절이 이 마일스톤의 스펙이다.

작업 범위:
1. ai/ 모듈: prompt_templates에서 initial_generation 로드 → 입력값 치환 → LLM 호출 → JSON 파싱. activity마다 lat/lng 근사 좌표를 반환하도록 프롬프트 본문 작성(시드 placeholder 교체). include_nearby=true면 근교 지역까지 추천 범위를 넓히는 지시 포함. max_tokens/타임아웃은 duration_days 비례
2. cache/ 모듈: CLAUDE.md 5.3절 정규화 규칙 그대로 캐시 키 생성(SHA-256), 조회(30일 lazy expiry)/upsert, 조회 실패 시 AI 직접 호출 폴백
3. 생성 결과를 trips/itinerary_days/itinerary_activities에 저장, trip_revisions에 revision=1 스냅샷
4. 실패 처리: 호출/파싱 실패 구분 로깅 + 각 1회 재시도, 최종 실패 502 GENERATION_FAILED
5. frontend 연결: M3 폼 제출 → 로딩 체크리스트 표시 → 성공 시 일정표 페이지로 이동(일정표는 M5에서 만드니 임시로 JSON 덤프 표시)

계획 먼저. 완료 기준: 동일 조건 2회 요청 시 2번째는 캐시 히트(AI 미호출 로그 확인), include_nearby 여부로 캐시 키가 달라짐, 커밋.
```

## M5. 일정표 화면 (지도+타임라인) UI (Day 5) — 2턴 구조

**턴 1 (정적 목업):**
```
일정표 페이지를 더미 trip JSON으로 정적 목업부터 만들자. CLAUDE.md 6절 '일정표' 행이 스펙이다.

구성 순서(위→아래): 헤더(제목/기간/저장 버튼) → Google 지도 → Day 탭 → 테마+변경됨 배지 → route_warning 참고 배지(6절 고정 문구/톤) → 타임라인 → 하단 [동선 최적화]+[재조정] 버튼.

지도 요구사항 (CLAUDE.md 2절: Marker+Polyline만, Directions API 금지):
- @vis.gl/react-google-maps 사용, 선택된 Day의 activities를 lat/lng로 마커 표시
- 마커는 파란 원+흰 숫자, 번호는 타임라인 순번과 동일
- 마커 순서대로 파란 Polyline 연결. route_warning flagged면 문제 구간을 노란 점선으로 구분(구간 특정이 어려우면 전체 경로 노랑 처리로 단순화)
- Day 탭 전환 시 해당 Day 마커/동선으로 갱신, fitBounds로 화면 맞춤

타임라인: 좌측 시간+순번 뱃지+연결선, 카드(제목/카테고리 태그/소요시간/비용/설명), 카드 사이 이동시간 텍스트. dnd-kit으로 같은 Day 내 드래그 재정렬(로컬 상태만) — 드래그 중 카드는 파란 점선+연한 배경, 순서 바뀌면 지도 마커/동선도 즉시 갱신.

계획 먼저, 구현 후 확인받고 커밋.
```

**턴 2:**
```
M5 목업을 실제 API에 연결하자. GET /api/trips/{trip_id} 백엔드 구현(응답은 docs/data-spec.md 스키마, last_modified는 itinerary_days.last_modified_at과 trips.updated_at 비교로 계산) + 프론트 react-query 연결. M4의 생성 완료 후 이 페이지로 이동하는 흐름까지. 계획 먼저, 완료 후 커밋.
```

## M6. 재조정 + 부분 재생성 검증 (Day 6~7, 핵심)

```
PATCH /api/trips/{trip_id}/reorder를 구현하자. 이 프로젝트의 핵심 마일스톤이고, CLAUDE.md 5.1, 5.2절이 스펙이다.

작업 범위:
1. 요청 검증: day 범위 밖 400, new_activity_order가 해당 day의 activity_key 집합과 불일치 시 400
2. ai/ reorder 프롬프트(시드 placeholder 교체): 변경 day 정보+새 순서만 전달, "다른 날짜는 원본 그대로 반환" 강제, route_warning 판정(좌표 기반 이동 거리 룰 체크 + 자연어 사유) 포함
3. PartialRegenerationValidator (독립 클래스): 응답에서 요청하지 않은 day의 activities를 원본과 깊은 비교 → 다르면 위반. 위반 시 1회 재시도, 재실패 502. **이 클래스만 단위 테스트 작성** (통과/위반/재시도 케이스)
4. 성공 시: 해당 day 갱신, revision +1, trip_revisions 스냅샷, route_warning 컬럼 저장
5. frontend: 드래그 후 [재조정] 클릭 → PATCH 호출 → 응답으로 화면 갱신(변경 day에 '변경됨' 배지, flagged면 참고 배지+지도 노랑 구간). [동선 최적화] 버튼은 flagged day에 대해 재조정을 다시 요청하는 동작으로 연결

계획 먼저. 완료 기준: 재조정 2회 반복 시 변경이 누적되고 다른 날짜는 그대로(DoD 항목), 검증 테스트 통과, 커밋.
```

## M7. 저장 목록/상세 + 빈 상태 (Day 8 오전)

```
GET /api/trips (내 목록, 최신순)와 '내 여행' 페이지를 만들자. CLAUDE.md 6절 '저장한 여행 목록' 행이 스펙이다.

1. 백엔드: GET /api/trips — 본인 것만, updated_at 최신순. 목록 응답은 카드에 필요한 필드만(trip_id, destination, summary, start_date, end_date, duration_days, preferences)
2. 목록 카드: 목적지 썸네일 블록(목적지 문자열 해시→팔레트 색 자동 배정)+아이콘, 제목, D-day 배지, 기간("7.10 – 7.12 · 2박 3일"), 취향 칩. revision·수정 횟수는 절대 노출하지 말 것
3. 빈 상태: CLAUDE.md 6절 고정 문구 그대로 — "일정이 존재하지 않네요. 일정을 만들까요?" + [AI 추천 일정 만들기] → 생성 폼 이동
4. 카드 클릭 → 일정표 페이지(M5) 재열람, 이어서 드래그/재조정 가능해야 함
5. 헤더 내비게이션: 내 여행 / 새 일정 / 로그아웃 정리

계획 먼저, 완료 후 커밋.
```

## M8. 관리자 3화면 (Day 8 오후)

```
관리자 기능을 구현하자. CLAUDE.md 4절 admin 엔드포인트 4종 + 6절 '관리자 3화면' 행이 스펙이다.

1. GET /api/admin/flagged-trips: itinerary_days.route_warning_flagged=true인 trip/day 목록(사유, 일시 포함)
2. GET /api/admin/stats/destinations: GROUP BY destination 집계(최근 30일 필터)
3. GET/PUT /api/admin/prompt-templates(+/{id}): 조회/수정, 수정 시 version +1
4. 프론트 관리자 페이지: 상단 요약 지표 카드 3장(생성 수, flagged 수·비율, 활성 템플릿 수) → 모니터링 테이블(행 클릭 시 해당 trip 일정표 읽기 전용 뷰) → 인기 목적지 가로 막대 → 템플릿 카드(활성 버전 배지, 편집 폼, 버전 이력)
5. 일반 user 계정으로 admin 화면 라우트 접근 시 차단(API 403과 별개로 프론트 가드)

계획 먼저, 완료 후 커밋.
```

## M9. 문서화 + 배포 + DoD 점검 (Day 9)

```
마무리 단계다.

1. Swagger: 모든 엔드포인트가 CLAUDE.md 4절 계약(요청/응답/에러 형식)대로 문서화됐는지 점검·보강, springdoc UI 경로 확인
2. 배포 준비: backend Docker 이미지 Render 배포 설정(render.yaml 또는 가이드), frontend Vercel 설정(vercel.json, 환경변수 목록), CORS를 Vercel 도메인으로 제한
3. 엣지케이스 점검: PRD 10절 항목별로 실제 동작 확인 스크립트/체크 결과 정리
4. PRD 13절 완료 기준(DoD) 전체를 체크리스트로 만들어 항목별 통과/미통과와 근거를 보고해줘. 미통과 항목은 수정까지.

배포 자체(콘솔 조작)는 내가 하니, 필요한 설정 파일과 단계별 가이드(README에 정리)를 만들어줘. 계획 먼저.
```

---

## 진행 중 자주 쓸 보조 프롬프트

- UI가 어긋났을 때: `스크린샷 첨부. CLAUDE.md 7절 토큰과 6절 스펙 기준으로 어디가 어긋났는지 먼저 진단하고, 수정 계획을 보여줘.`
- 컨텍스트가 길어졌을 때: `/compact 지금까지의 결정사항과 미완료 작업만 남겨줘`
- 마일스톤 완료 확인: `이번 마일스톤의 완료 기준을 하나씩 실제로 검증하고 결과만 표로 보고해줘. 통과 못한 건 수정해.`
